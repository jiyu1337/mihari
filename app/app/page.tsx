import { GuardianConsole } from "@/components/guardian-console";

export const metadata = {
  title: "Event Register — MIHARI",
};

export default function AppPage() {
  return <GuardianConsole />;
}
